#include "main.h"

/**
 * print_last_digit - Entry point
 * @n: n is an integer
 * Return: 0
 */
int print_last_digit(int n)
{
	int i = n % 10;

	if (n < 0)
		i = i * -1;

	_putchar(a + '0');
	return (i);
}
